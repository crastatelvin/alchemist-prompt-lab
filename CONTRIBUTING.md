# Contributing

1. Create a feature branch.
2. Run backend tests: `pytest backend/tests`
3. Run frontend tests: `npm run test --prefix frontend`
4. Run lint checks:
   - `ruff check backend`
   - `npm run lint --prefix frontend`
5. Open a pull request with a clear test plan.
